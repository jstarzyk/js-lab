doctype html
html
    head
        title Pierwsza strona
    body
        h1 Schema List
        form#formAddUser(name="addsubject",method="post",action="/addsubject")
            input#inputName(type="text", placeholder="name", name="name")
            input#inputAssignments(type="list", placeholder="assignments", name="assignments")
            button#btnSubmit(type="submit") submit
